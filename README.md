# PatientBank Support Site

This repository stores the support pages for PatientBank, which we compile with
Grunt.

Installation:

    npm install

Usage (this will watch for changes to assets and re-compile them):

    grunt
